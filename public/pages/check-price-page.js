import { domElements } from "../globals.js";

export function renderCheckPricePage() {
  const html = `
    <div class="js-check-price-container check-price-container">
      
    </div>
  `;

  domElements.mainContentDOM.innerHTML = html;
}